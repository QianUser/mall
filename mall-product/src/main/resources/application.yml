spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.227.131:3306/mall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: mall-product
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  thymeleaf:
    cache: true
  redis:
    host: 192.168.227.131
    port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 3600000
#      key-prefix: CACHE_  # 如果不指定，则默认使用缓存名字作为前缀
      use-key-prefix: true
      cache-null-values: true  # 缓存空值（例如当方法返回null时）

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0

server:
  port: 10001